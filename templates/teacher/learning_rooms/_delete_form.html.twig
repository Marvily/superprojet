<form method="post" action="{{ path('learning_rooms_delete', {'id': masterclass.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ masterclass.id) }}">
    <button class="btn btn-outline-danger float-right">Delete</button>
</form>
