{% extends 'admin/base.html.twig' %}

{% block title %}Discipline{% endblock %}

{% block content %}
<div class="col-12"><a href="{{ path('admin_discipline_index') }}" class="btn btn-outline-primary mb-3">Back to list</a></div>
<div class=" m-auto shadow p-3">
    <table class="table ">
        <tbody>

            <tr>
                <th>Titre</th>
                <td>{{ discipline.titre }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ discipline.niveau }}</td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('admin_discipline_edit', {'id': discipline.id}) }}" class="btn btn-outline-primary">edit</a>
</div>
    <div class="col-12 mt-3">  {{ include('admin/discipline/_delete_form.html.twig') }}</div>

{% endblock %}
