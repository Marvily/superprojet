<form method="post" action="{{ path('admin_discipline_delete', {'id': discipline.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ discipline.id) }}">
    <button class="btn btn-outline-danger float-right">Delete</button>
</form>
